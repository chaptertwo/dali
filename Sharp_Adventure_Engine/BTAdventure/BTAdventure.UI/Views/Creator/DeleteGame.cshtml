@model BTAdventure.Models.Game
@{
    ViewBag.Title = "DeleteGame";
}

<h2>DeleteGame</h2>
<hr />
<br />
@using (Html.BeginForm("DeleteGame", "Creator", FormMethod.Post))
{

    //Html.HiddenFor(m => m.GameId);

    //Html.HiddenFor(m => m.GameTitle);
    //Html.HiddenFor(m => m.IntroText);
    <input type="hidden" name="GameId" value="@Model.GameId" />
    <input type="hidden" name="GameTitle" value="@Model.GameTitle" />
    <input type="hidden" name="IntroText" value="@Model.IntroText" />
    <p>Are you sure you want to delete @Model.GameTitle?</p>
    <button type="submit" class="btn btn-primary">Yes</button>

}
@Html.ActionLink("No", "Index", null, new { @class = "btn btn-primary" })